import { styled } from 'styled-components';
import { Badge } from '@/ui/components/badge';

export const ActionMessageStyled = styled.div`
  display: flex;
  justify-content: center;
  width: 100%;
`;

export const ActionMessageContent = styled.div``;

export const ActionMessageBadge = styled(Badge)``;
