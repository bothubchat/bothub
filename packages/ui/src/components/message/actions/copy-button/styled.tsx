import { animated } from '@react-spring/web';
import { styled } from 'styled-components';

export const MessageActionsButtonIconStyled = styled(animated.div)`
  display: flex;
  align-items: center;
  justify-content: center;
  width: 18px;
  height: 18px;
`;
