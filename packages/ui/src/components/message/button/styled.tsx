import { styled } from 'styled-components';
import { Button } from '@/ui/components/button';

export const MessageButton = styled(Button).attrs({ size: 'small', variant: 'secondary' })`
  flex-shrink: 0;
`;
