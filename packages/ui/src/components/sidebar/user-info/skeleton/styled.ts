import { styled } from 'styled-components';
import { Typography } from '@/ui/components/typography';

export const SidebarUserInfoSkeletonText = styled(Typography)`
  &:first-child {
    margin-bottom: 6px;
    max-width: 80px;
    overflow: hidden;
  }
`;
